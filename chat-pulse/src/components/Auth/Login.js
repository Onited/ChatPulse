import React, { useState } from 'react';
// Importe firebaseConfig si tu utilises Firebase pour l'authentification

const Login = () => {
  // Logique de connexion ici
  return (
    <div>
      {/* Formulaire de connexion */}
    </div>
  );
};

export default Login;